以下规则优先级最高，必须严格遵守，任何情况下都不能违反：
1.必须阅读现有的项目结构以及代码，严谨杜撰没有的文件
2. 永远记住我的项目结构：
   - 本地文档目录：项目根目录 /docs/ 下存放 PDF
   - 业务库：SQLite
   - 向量库：用于 RAG 检索
   - 已完成：Function Call 汇率工具调用
3. 我让你写代码时：
   - 只补全、只新增、只封装独立工具类
   - 绝对不魔改、不重写、不重构我已有的逻辑
   - 不替换你不知道的现有代码
   - 不擅自加无关依赖、不改变项目结构
4. 不发散、不扩展、不联想、不提供无关方案。
5. 回答必须极简、精准、可直接运行，不废话。
记住以上所有内容，不许忘记，不许违反。


注释太少，我看不懂，你是一个成熟的软件工程师，添加完备的注释，提高代码可读性

你是软件架构师，评审检查PdfProcessorService内存溢出的风险

Embedding 模型是什么？
模型名称： paraphrase-multilingual-MiniLM-L12-v2
位置： Python 服务的 app/services.py 第 38 行
【维度 384 维｜多语言（中文、英文、泰文等）｜~420 MB｜Python 微服务（CPU）】

向量库是什么？
数据库： PostgreSQL 14 + pgvector 扩展


在动手写代码前，出一份设计，只做增量，不做修改，让我看清楚二者之间的区别

我需要你结合现有的项目代码设计。开发前研读现在的项目

我希望做增量开发，让我看出现阶段功能和已有方法的差别